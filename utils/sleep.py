import time


def sleep(number: int) -> None:
    """Function responsible for make a pause in code. Base on a integer.

    Parameters:
            number: integer number that
                going to pause a code for a interval of seconds.

    Returns:
            None
    """
    time.sleep(number)
