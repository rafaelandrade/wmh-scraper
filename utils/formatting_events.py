def formatting_events(event: str) -> str:
    """
    Function responsible for formatting events to lower case.

    Parameters:
        event: str

    Returns:
        str
    """
    return event.lower()
